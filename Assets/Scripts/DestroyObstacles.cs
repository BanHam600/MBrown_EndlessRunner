using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyObstacles : MonoBehaviour
{

    private GameObject obstacle;
    
    // Update is called once per frame
    void Update()
    {
        if (obstacle.transform.position = 
    }
}
